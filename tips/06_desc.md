By default, results of query are in ascending order.</br>
Remember that by default, one grows from small to large in life, that's why default order is `ASEC`.</br>

If you want the other way, try `DESC`.
